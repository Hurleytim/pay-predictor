import React from 'react'
import ReactDOM from 'react-dom/client'
import PayPredictor from './PayPredictor'
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode><PayPredictor /></React.StrictMode>
)
